# digital-clock-c-program
A menu-driven C program that represents a digital clock using structures. It allows the user to display time, validate the entered time, convert 24-hour time to 12-hour format, and add seconds to the current time.
